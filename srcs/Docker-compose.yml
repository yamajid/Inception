version: '3.8'

services:
  nginx:
    build:
      context: ./requirements/nginx
    ports:
      - "443:443"
    networks:
      - wp_network
    volumes:
      - /home/yamajid/data/wordpress_data:/var/www/html
    restart: always
    depends_on:
      - wordpress
  wordpress:
    build:
      context: ./requirements/wordpress
    env_file:
      - .env
    volumes:
      - /home/yamajid/data/wordpress_data:/var/www/html
    networks:
      - wp_network
    restart: always
    depends_on:
      - mariadb
  mariadb:
    build:
      context: ./requirements/mariadb
    env_file:
      - .env
    volumes:
      - /home/yamajid/data/mariadb_data:/var/lib/mysql
    networks:
      - wp_network
    restart: always

networks:
  wp_network:
    driver: bridge

volumes:
  wordpress_data:
    name: wordpress_data
  mariadb_data:
    name: mariadb_data